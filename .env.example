# Create a key in Google AI Studio. Never commit the real key.
GEMINI_API_KEY=replace_with_your_demo_key

# Free-tier friendly default as of the current prototype; override if availability changes.
GEMINI_MODEL=gemini-3.5-flash-lite
